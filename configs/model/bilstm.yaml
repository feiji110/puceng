_target_: src.models.puceng_module.PUCENGLitModule

optimizer:
  _target_: torch.optim.AdamW
  _partial_: true
  lr: 0.003
  weight_decay: 0.01

scheduler:
  _target_: torch.optim.lr_scheduler.ReduceLROnPlateau
  _partial_: true
  mode: min
  factor: 0.1
  patience: 10

net:
  _target_: src.models.components.BidirectionalLSTM_net.BidirectionalLSTM
  dropout_rate: 0.25 


# compile model for faster training with pytorch 2.0
compile: false
