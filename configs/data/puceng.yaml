_target_: src.data.puceng_datamodule.PUCENGDataModule
data_dir: ${paths.data_dir}
batch_size: 64 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
points: -1   # mean all data
train_val_test_split: [0.8, 0.15, 0.05]
num_workers: 24
pin_memory: False
repreprocess: False